<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation_person"
    app:startDestination="@+id/navigation_feed">

    <fragment
        android:id="@+id/navigation_feed"
        android:name="pt.vow.ui.feed.FeedFragment"
        android:label="@string/title_feed"
        tools:layout="@layout/fragment_feed" />

    <!--<fragment
        android:id="@+id/navigation_map"
        android:name="pt.vow.ui.maps.MapsFragment"
        android:label="@string/title_map"
        tools:layout="@layout/fragment_maps" />-->

    <fragment
        android:id="@+id/navigation_choose_type"
        android:name="pt.vow.ui.newActivity.NewActivityMapFragment"
        android:label="@string/title_select_activity_dest"
        tools:layout="@layout/fragment_new_activity_map" />

    <fragment
        android:id="@+id/navigation_podium"
        android:name="pt.vow.ui.podium.PodiumFragment"
        android:label="@string/title_podium"
        tools:layout="@layout/fragment_podium" />

    <fragment
        android:id="@+id/navigation_profile"
        android:name="pt.vow.ui.profile.ProfileFragment"
        android:label="@string/title_profile"
        tools:layout="@layout/fragment_profile" />

</navigation>